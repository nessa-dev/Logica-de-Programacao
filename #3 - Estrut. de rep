/******************************************************************************

Modifique o algoritmo anterior, de maneira que o usuário também informe 
o início e o fim da tabuada. Por exemplo: usuário solicitando a tabuada 
do número 6, com início em 5 e fim em 8. 

*******************************************************************************/

#include <stdio.h>

int main()
{
   int num, i, inicio, fim; 
    
   printf("Digite um número de 1 - 10:\n");
   scanf("%d", &num);
   printf("Informe em qual número vamos iniciar:\n");
   scanf("%d", &inicio);
   printf("Informe em qual número vamos terminar:\n");
   scanf("%d", &fim);
    
 for(i = inicio; i <= fim; i++) {
       printf("%d X %d = %d\n", num, i, num * i);
   }

   return 0;
}
